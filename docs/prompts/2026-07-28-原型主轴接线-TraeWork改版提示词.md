# 原型主轴接线 · Trae Work 改版提示词

> 用法：将下方「提示词正文」整段复制到 Trae Work。  
> 若工具能读本地文件，请同时打开提示词中列出的路径。  
> 本轮目标：**把已有页面串成可走通的整体流程**，不是再新开一个平行演示页。  
> **归档注记（2026-07-28）：** `prototype/pages/workbench.html` **已删除**；现入口为 `home.html`，主题壳为 `theme-overview.html` / `theme-work.html`。下文「工作台」指当时枢纽，勿再当作现存文件。

---

## 提示词正文（复制从下一行开始）

```
你是产品/交互设计师兼前端实现者。请基于现有高保真原型，完成「整体流程主轴接线」改版：让用户从主题工作台出发，能走通创建/拦截 → 计划共创 → 执行 → 复盘 → 阶段切换，而不是打开一堆彼此无关的 HTML。

【背景矛盾】
- 现状：五页各自可演示，但页面之间几乎没有可走通入口；用户不知道从哪进。
- 根因：最近几次是局部加页（对话共创、阶段与并发），没有回写第一版工作台主轴；.design 声明的交互与 DOM 不一致；直开 HTML 时缺少真实相对链接。
- 逻辑冲突：工作台 P 栏仍是「学习/练习/应用」三入口并排（旧模型）；07-27 已定「当前阶段 + 当前切片 + 历史归档」，该能力在 theme-plan-phases.html 做成了平行页。
- 已验收结论：整体流程不通过；单页能力局部可用。本轮必须修「串得起来」，不是再加功能面。

【产品锚点】
- 总纲：docs/requirements/2026-07-23-可视化刻意练习系统-需求澄清.md
- 阶段/并发：docs/requirements/2026-07-27-主题计划阶段与并发-需求澄清.md
- 设计说明：docs/specs/2026-07-27-主题计划阶段与并发-design.md
- ~~信息架构枢纽：prototype/pages/workbench.html~~（**已删除**；现枢纽：home / theme-overview / theme-work）
- 已有模块页（复用，勿重做整页视觉）：
  - prototype/pages/learning-plan-cocreate.html（学习切片对话共创）
  - prototype/pages/theme-plan-phases.html（阶段切换 / 槽位 / 主焦点 / 漂移）
  - prototype/pages/new-theme-intercept.html（新建主题拦截）
  - prototype/pages/weekly-review.html（周复盘）
- 画布声明：prototype/.design（必须与 DOM 的 data-dom-id 对齐）
- 必须延续现有 Trae 设计体系（dark token、components、图标路径），不要另起视觉语言。

【目标主路径（必须可走通）】
用户以 ~~workbench~~（**已删除**；现以 `home.html`）为唯一入口，按下列顺序能点通（浏览器直开 HTML 也要能跳，不能只依赖 Trae 画布）：

1. 工作台 →「新建主题」→ new-theme-intercept → 返回工作台
2. 工作台 P · 当前学习切片 →「共创 / 打开计划」→ learning-plan-cocreate →「锁定」后返回工作台（工作台显示已锁定）
3. 工作台 D · 今日任务可见（示意来自当前 active 切片即可）
4. 工作台 C ·「开始复盘」可点 → weekly-review → 返回工作台（演示态请开放，不要 disabled）
5. 工作台 P ·「进入下一阶段」或等价入口 → 进入阶段切换能力（优先：把阶段切片 UI 并入/替换工作台 P；若必须保留独立页，则从工作台真跳转 theme-plan-phases，且该页有明确「返回工作台」）
6. 阶段满槽硬拦、主焦点>1 警告：可从工作台侧栏或 P 区看见入口；叙事与拦截页一致（对齐 1+3+5，勿再暗示「有未完成主题就禁止一切」而不说腾槽）

【本轮必须改动】
A. ~~改造 workbench.html 的 P · 计划栏~~（workbench **已删除**；现改 theme-overview / theme-work）
   - 删除或降级「学习计划 / 练习计划 / 应用计划」三折叠并排主展示。
   - 改为：当前阶段徽章 + 当前切片（可编辑清单摘要）+ 历史切片折叠只读 + 主操作「打开共创/编辑」「进入下一阶段」。
   - 示例主题仍用「革新学习方法」；可暗示另有练习续跑主题（如「矛盾论」）以对齐并发叙事。

B. 接线（真实相对链接 + data-dom-id 双轨）
   - 每个跨页按钮/链接：既有 data-dom-id（供 .design），又有可用的相对 href（如 ../pages/xxx.html 或同目录 xxx.html），保证浏览器直开也能跳。
   - 必须存在并可用的 id（名称可微调但须写入 .design）：
     - btn-new-theme → new-theme-intercept.html
     - btn-open-plan-cocreate（或等价）→ learning-plan-cocreate.html
     - btn-start-review → weekly-review.html（去掉 disabled）
     - btn-theme-plan-phases 或把阶段能力内嵌后删除对该孤儿跳转的虚假声明
     - 各子页返回 → ~~workbench.html~~（**已删除**；现回 theme-work / home）
   - 更新 prototype/.design：登记 learning-plan-cocreate；删除「声明了但 DOM 不存在」的交互；保证往返对称。

C. 轻量统一叙事（不做大改文案工程）
   - 工作台可见：当前阶段、槽位占用摘要（如 学1/1 练1/3 用0/5）、主焦点数量。
   - 漂移：可继续保留「今日任务未完成」提示，但至少有一处能链到/展示「主焦点>1」类漂移（可跳阶段页漂移列表）。
   - 拦截页：若文案仍像旧规则，改为与槽位硬拦一致的腾槽说明（切换阶段 / 休眠 / 废弃）。

【交互硬约束】
- 工作台仍是 PDCA 四栏壳；不要拆成多套首页。
- 阶段切换仅手动；满槽硬拦；completed 切片只读。
- 共创页、阶段页、复盘页：以接线与入口为主，不要重做整套对话剧本/整套阶段逻辑（已有可交互部分保留）。
- 桌面优先；窄屏可堆叠。
- 假数据前端模拟即可；不接真实 LLM/后端。

【验收清单（交付前自测）】
从 ~~workbench.html~~（**已删除**；现从 `home.html`）用浏览器走一遍，下列全部成立才算完成：
1. 不打开 Trae 画布，仅靠点击链接，能到达共创、拦截、复盘、阶段（或内嵌阶段）四类能力，并都能回到工作台。
2. 工作台 P 不再以三份计划并排为主模型。
3. 「开始复盘」可点。
4. .design 中每个 interactions.domId 在对应 HTML 中真实存在；learning-plan-cocreate 已登记。
5. 用户能说清：我现在在哪个主题、哪个阶段、下一步该点哪里。

【本轮唯一任务】直接改现有原型并交付。
请产出：
1. 更新后的 HTML（至少 ~~workbench~~（**已删除**）；现至少 home / theme-overview / theme-work；按需改其余四页与 .design）
2. 一段「如何走通主路径」的点击说明书（≤15 步）
3. 变更摘要：改了哪些入口/删了哪些旧三入口
4. ≤8 条假设 + 仍未接线的次要项（若有）

【明确不要】
- 不要再新建第五个平行「总览/导航」演示页代替接线。
- 不要重做整套视觉；不要低保真线框作为主交付。
- 不要重写共创对话引擎或复盘引擎。
- 不要实现阶段回退、真实后端、多用户权限。
- 不要保留「.design 有、页面无」的虚假交互。

【语言】回复与界面文案使用简体中文。
```
